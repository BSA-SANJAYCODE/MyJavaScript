const letter="India";
for (const element of letter) 
    {
     if(element==='I')
     {
        console.log(`Capital letter in india= ${element}`);
        break
     }
     console.log(element);
     
}